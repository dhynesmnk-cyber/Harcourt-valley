# dependencies
node_modules/

# build output — never commit, Netlify rebuilds it
dist/

# env files — keep keys out of git entirely
.env
.env.*
!.env.example

# logs & editor cruft
*.log
npm-debug.log*
.DS_Store
.vscode/
.idea/
.next/
build/
